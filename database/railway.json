{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "database/liquibase.Dockerfile",
    "watchPatterns": ["database/**"]
  },
  "deploy": {
    "restartPolicyType": "NEVER"
  }
}

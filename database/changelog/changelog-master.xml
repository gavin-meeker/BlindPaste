<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
    <!--
      Thin orchestrator only. All schema changes live as plain-SQL changesets in
      ./changesets and are applied in filename order, so use a sortable,
      zero-padded prefix: 0001_create_ping.sql, 0002_add_xyz.sql. Each .sql file
      begins with a Liquibase formatted-SQL header line ("liquibase formatted
      sql") followed by one "changeset author:id" line per change, and ends with
      its own "rollback" line(s). (Those header lines use a double-dash prefix,
      which cannot be shown inside this XML comment.)

      Only put changesets in ./changesets - includeAll runs every .sql it finds
      there as a forward migration.

      Every changeset must be idempotent: running the file a second time against a
      database that already has it must succeed and change nothing. Use CREATE TABLE
      IF NOT EXISTS, CREATE INDEX IF NOT EXISTS, ALTER TABLE ... ADD COLUMN IF NOT
      EXISTS, and seed rows with INSERT ... WHERE NOT EXISTS or ON CONFLICT DO
      NOTHING. Liquibase's own tracking table already stops a changeset running twice
      in normal operation, so this is about the cases where that tracking is not in
      play - a restored database, a hand-run file, a cleared checksum, and the test
      fixture in tests/BlindPaste.Api.Tests, which applies these files directly. There
      is a test that applies every changeset twice and fails if one of them is not
      idempotent.
    -->
    <includeAll path="changesets/" relativeToChangelogFile="true" />
</databaseChangeLog>

meta {
  name: Health
  seq: 1
}

docs {
  Liveness only. Run this first — if it fails, nothing below is worth reading, and the
  failure is almost always the API not being up or pointing at a database that is not.
}
